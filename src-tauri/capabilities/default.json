{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window can select repositories and receive change events",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
